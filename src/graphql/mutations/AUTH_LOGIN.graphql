mutation AuthUserLogin($input: AuthInput) {
    authUserLogin(input: $input) {
        error {
            message
            code
        }
        token
        user {
            _id
            availableCredit
            bettingStatus
            createdAt
            creditLimit
            loginStep
            password
            role
            status
            userName
            transferStatus
        }
    }
}
